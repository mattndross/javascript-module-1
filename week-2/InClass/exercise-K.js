function printStudents(array) {
    for (i = 0; i < array.length; i++) {
        console.log(array[i]);
    }
}

const students = ['Camilo', 'Ana', 'Jorge', 'Tamara', 'Pepe'];

console.log(printStudents(students));